[pytest]
# Modules live at the package root and in tests/; conftest.py puts both on
# sys.path. pythonpath is repeated here so editor / CI runners that read pytest
# config but skip conftest still resolve the flat imports.
pythonpath = . tests
testpaths = tests
python_files = test_*.py
